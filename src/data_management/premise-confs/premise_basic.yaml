

# Data split
data_split: "splits/final-split.json"

# Location of partitioned files from coq-lsp raw data
data_loc: "raw-data/coq-dataset"

# Location of sentence db
sentence_db_loc: "sentences.db"

# Directory to save the processed premise selection data
output_dataset_loc: "data/prem-select-2"

# Number of negative premises to use per positive premise
num_negatives: 3

# Number of the negative premises that should come from the same 
# file as the theorem 
base_wrapper:
  alias: local
  vector_db_loc: vector-dbs/prem-select
  checkpoint_loc: models/prem-select/checkpoint-15000
