[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "coq-modeling"
version = "0.0.1"
dependencies = [
    "torch",
    "transformers",
    "tqdm",
    "trl",
    "peft",
    "jsonlines",
    "sentencepiece",
    "protobuf",
    "matplotlib",
    "accelerate",
    "bitsandbytes",
    "scipy",
    "scikit-learn",
    "tsnecuda",
    "pyyaml",
    "flask",
    "pytest",
    "hypothesis",
    "evaluate",
    "numpy",
    "termcolor",
    "openai",
    #"pytorch-lightning",
    #"jsonargparse[signatures]",
    #"pandas",
    "types-PyYAML",
    "types-requests",
    "black",
    #"deepspeed",
    "GitPython",
    "typeguard",
    "ipdb",
    "parsy",
    "edist",
]

[tool.setuptools.packages.find]
where = ["src"]
