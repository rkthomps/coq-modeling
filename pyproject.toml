[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "coq-modeling"
version = "0.0.1"
dependencies = [
    "torch",
    "openai",
    "transformers",
    "tqdm",
    "trl",
    "peft",
    "jsonlines",
    "sentencepiece",
    "protobuf",
    "matplotlib",
    "accelerate",
    "bitsandbytes",
    "scipy",
    "pyyaml",
    "pytest",
    "json-rpc",
    "hypothesis",
    "evaluate",
    "numpy",
    "termcolor",
    "openai",
    "types-PyYAML",
    "types-requests",
    "black",
    "GitPython",
    "typeguard",
    "ipdb",
    "parsy",
    "edist",
]

[tool.setuptools.packages.find]
where = ["src"]
